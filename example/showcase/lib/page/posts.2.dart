import 'package:flutter/material.dart';

//! websocket

class Posts2Page extends StatelessWidget {
  const Posts2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
