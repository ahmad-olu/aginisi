import 'package:flutter/material.dart';

//! filters , relation. e.t.c

class Posts4Page extends StatelessWidget {
  const Posts4Page({super.key, required this.type});
  final String type;

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
