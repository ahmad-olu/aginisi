import 'package:flutter/material.dart';

// ! socket io

class Posts3Page extends StatelessWidget {
  const Posts3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
