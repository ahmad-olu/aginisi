import 'package:flutter/material.dart';

//! get method

class Posts1Page extends StatelessWidget {
  const Posts1Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
